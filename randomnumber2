package randomnumber2;
import java.util.Random;
import java.util.Scanner;

/**
 *
 * @author tvandillen2028
 */
public class RandomNumber2 {
   


    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
   Scanner scanner = new Scanner(System.in);
   Random random = new Random();
   
   System.out.println("-----6 sided Dice Generator-----");
    String input = ("r");
    
    while (input.equalsIgnoreCase("r")) {
            // Generate a random number between 1 and 6
            int dieRoll = random.nextInt(6) + 1;
            
            System.out.println("\nYou rolled a: " + dieRoll);
            
            // Ask user to roll again or quit
            System.out.print("Type 'r' to roll again, or any other key to exit: ");
            input = scanner.next();
        }
        
        System.out.println("\nThanks for playing!");
        scanner.close();
    }
}
